version: 2.1
jobs:
  build-and-push:
    docker:
      - image: circleci/openjdk:8u242-jdk
    steps:
      - checkout
      - setup_remote_docker
      - run: mvn dependency:go-offline
      - run:
          name: Build
          command: mvn clean package -Dmaven.test.skip=true -Ddocker.image.tag=Build-${CIRCLE_BUILD_NUM}-CI -Ddockerfile.maven.settings.auth=false
      - run:
          name: Login to Dockerhub
          command: docker login -u $DOCKER_USER -p $DOCKER_PASS
      - run:
          name: Push
          command: mvn dockerfile:push -Ddocker.image.tag=Build-${CIRCLE_BUILD_NUM}-CI
workflows:
  build-flow:
    jobs:
      - build-and-push